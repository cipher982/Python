'''
Read a string and count the occurrences of each word.
'''

import numpy as np
spams = "offer is secret click secret link secret sports link play sports today went play sports secret sports event sport is today sport costs money"
print np.size(spams)

count = {}
for w in spams.split():
    if w in count:
        count[w] += 1
    else:
        count[w] = 1
for word, times in count.items():
    print "%s was found %d times" % (word, times)
